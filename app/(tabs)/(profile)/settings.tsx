import { StyleSheet, Text, View , SafeAreaView } from 'react-native'
import React from 'react'

const Settings = () => {
  return (
    <SafeAreaView>
      <Text style={{color:'white',fontSize:50}}>settings</Text>
    </SafeAreaView>
  )
}

export default Settings

const styles = StyleSheet.create({})